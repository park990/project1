<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--수강중인 학생-->
<mapper namespace="lec_std_t"> <!--테이블 명 or 파일명-->

    <!--선영수정 -->
    <select id="enrolledStudent" resultType="pm.vo.EnrolledStudentVO" parameterType="Map">
        SELECT es.lec_no, es.stdno, es.lec_stdno, es.std_name, es.lec_name, s.std_phone
        FROM lec_std_t es INNER JOIN std_t s
        ON es.stdno = s.stdno
        WHERE lec_no = #{lec_no}
    </select>


    <!--수강중인 학생에 수강생 추가-->
    <insert id = "insertEnrolledStudent" parameterType="pm.vo.EnrolledStudentVO">
        INSERT INTO lec_std_t (
        lec_stdno, lec_name, stdno, std_name, lec_no
        )VALUES (
        #{lec_stdno}, #{lec_name}, #{stdno}, #{std_name}, #{lec_no}
        )
    </insert>

    <!--수강중인 학생에 수강생 제외-->
    <delete id = "deleteEnrolledStudent" parameterType="pm.vo.EnrolledStudentVO">
        DELETE FROM lec_std_t
        WHERE lec_stdno = #{lec_stdno}
    </delete>

    <!--선영수정 -->


</mapper>