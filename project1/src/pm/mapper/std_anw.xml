<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="std_anw">
    <insert id="anw" parameterType="pm.vo.std_anw_tVO">
        INSERT INTO std_anw_t (test_idx, ques_num, stdno, s_anw)
        VALUES (#{test_idx}, #{ques_num}, #{stdno}, #{s_anw})
    </insert>
    <select id="gets_anw" parameterType="pm.vo.testDTO" resultType="pm.vo.std_anw_tVO">
        SELECT std_anw_t_idx, ques_num, stdno, s_anw, test_idx
        FROM std_anw_t
        WHERE test_idx = #{test_idx} AND stdno= #{stdno}
    </select>
    <select id="getc_anw" parameterType="pm.vo.testDTO" resultType="pm.vo.test_anw_tVO">
        SELECT test_anw_t_idx, test_idx, ques_num, c_anw
        FROM test_anw_t
        WHERE test_idx = #{test_idx}
    </select>
</mapper>