<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test">

    <select id="getT" resultType="pm.vo.test_tVO" parameterType="String">
        SELECT test_idx, test_name, test_ques_num, lec_no
        FROM test_t
        WHERE test_idx = #{test_idx}
    </select>
    
    <select id="getQ" resultType="pm.vo.test_ques_tVO" parameterType="String">
        SELECT test_idx, ques_num, test_item_tnum, test_ques_pit, ques_con, ques_idx
        FROM test_ques_t
        WHERE test_idx = #{test_idx}
        ORDER BY ques_num
    </select>
</mapper>